//json_string_filter in ruby

Filtering the json string per line from the input file and storing the output in a multiple file according to the hashing modulo function on user_id.

This code is filtering all the Json strings with language=english and dividing it in 100 files

//code running direction
ruby filename.rb inputfile